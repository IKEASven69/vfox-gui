# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# Build output
dist
dist-portable
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri updater signing keys — the private key MUST never be committed.
# Keep it in a password manager / CI secret store only.
src-tauri/.tauri/
*.key

# Editor / agent tool metadata (not part of the project)
.codegraph/
.omo/
CLAUDE.md
